spring:
  application:
    name: agent_app
  ai:
    dashscope:
      api-key: sk-dd60cc093a40434d92225be6d056b31c
      chat:
        options:
          model: qwen-max-2025-01-25
      client:
        connect-timeout: 30s
        read-timeout: 60s
        write-timeout: 30s
  datasource:
    url: jdbc:mysql://1.13.183.61:13306/agent_app?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
server:
  port: 8123
  servlet:
    context-path: /api

mybatis:
  mapper-locations: classpath:mapper/*.xml

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.zqw.agent_app.controller

knife4j:
  enable: true
  setting:
    language: zh_cn